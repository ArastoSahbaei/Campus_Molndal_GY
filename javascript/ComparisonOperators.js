const barkisWeight = 550
const veteLangd = 700

const identical = barkisWeight == veteLangd
const identicalValueAndType = barkisWeight === veteLangd
const isDifferent = barkisWeight != veteLangd
const isDifferentValueAndType = barkisWeight !== veteLangd
const isGreater = barkisWeight > veteLangd
const isGreaterOrEqual = barkisWeight >= veteLangd
const isLess = barkisWeight < veteLangd
const isLessOrEqual = barkisWeight <= veteLangd

function comparisonOperator() {
	alert(isLessOrEqual)
}